# 黄历运势（Chinese Oracle）— 工程审计报告

| 字段 | 内容 |
|------|------|
| 报告日期 | 2026-08-10 |
| 工程目录 | `chinese-oracle-1.21.1` |
| 对应设计文档 | `国风每日运势-Mod设计文档.md` v1.0 |
| 本次目标 | 模板清理与改名（Phase 0）+ 服务器 MVP（Phase 1）+ 内容与道具（Phase 2） |
| 构建状态 | ✅ `./gradlew build` BUILD SUCCESSFUL |
| 冒烟测试 | ✅ 专用服务器启动成功，词库正常加载（9 吉凶 / 11 宜忌 / 40 签文） |

---

## 1. Phase 0 — 工程清理与改名（已交付）

### 1.1 命名

| 项 | 原值 | 现值 |
|----|------|------|
| 目录 | `examplemod-template-1.21.1` | `chinese-oracle-1.21.1` |
| mod_id | `examplemod` | `chinese_oracle` |
| mod_name | `Example Mod` | `Chinese Oracle` |
| Java 包 | `com.example.examplemod` | `com.tanrunn.chineseoracle` |
| 主类 | `ExampleMod` / `ExampleModClient` | `ChineseOracleMod` / `ChineseOracleModClient` |
| mixins | `examplemod.mixins.json` | `chinese_oracle.mixins.json` |
| 资源命名空间 | `assets/examplemod` | `assets/chinese_oracle` |

### 1.2 删除的模板残留

- 示例方块 / 方块物品（`example_block`）
- 示例食物物品（`example_item`）
- 示例创意标签（`example_tab`）
- 魔法数字 / dirt 日志等示例配置项
- `TEMPLATE_LICENSE.txt`

### 1.3 保留

- `gradlew` / `gradle/wrapper`（Java 21，Gradle 9.2.1）
- `.github/workflows/build.yml`（CI 编译）
- `.gitignore`、`.gitattributes`（`run/`、`build/` 均已忽略）

---

## 2. Phase 1 — 服务器 MVP（已交付）

### 2.1 源码结构与设计文档映射

| 设计文档章节 | 实现 |
|--------------|------|
| §4.3 common 模块 | `common/`：`ActionType`、`FortuneSnapshot`、`Modifier`（双端 DTO/常量） |
| §4.2 Domain / §5.1 | `server/fortune/FortuneData`：玩家持久化（`INBTSerializable<CompoundTag>`） |
| §5.1 存储 | NeoForge Player Attachment：`ChineseOracleMod.FORTUNE_DATA`（随玩家 dat 存档） |
| §6.1 dayIndex | `server/fortune/DayService`：overworld `getDayTime()/24000`，O(1)/tick 全局检测 |
| §6.2 补签/求签 | `server/fortune/FortuneService.ensureToday`（新玩家 dayIndex=-1 必求签） |
| §6.3 抽取管线 | `server/fortune/DrawPipeline`（纯函数、同 seed 可复现） |
| §6.3 seed | `HashCommon.mix(worldSeed ^ uuid ^ dayIndex ^ SALT)` |
| §6.4 日切尖峰 | `server/fortune/RolloverQueue`：`rolloverPlayersPerTick` 预算分片 |
| §6.3 连凶保护 | `FortuneRegistry.drawTier`：昨日大凶则避开大凶档（`badProtection` 可关） |
| §5.2 预计算 | `server/fortune/ModifierTable` + `FortuneService` 运行时缓存（UUID→snapshot+modifier） |
| §5.4 词库 | `server/registry/FortuneRegistry` + `FortuneReloadListener`（datapack JSON 加载） |
| §5.4 热重载 | `/oracle reload`：重建不可变 Registry 快照（copy-on-write 原子替换）+ 清缓存 |
| §3.7 命令 | `command/OracleCommand`：`/oracle me`、`/oracle player`、`/oracle set`、`/oracle reroll`、`/oracle reload`、`/oracle debug` |
| §4.2 Integration / §8.4 | `server/hook/FortuneHooks`：登录补签、日切入队、退出清理、挖矿/战斗钩子 |
| §9.2 叠加规则 | ModifierTable 合成时合并 + `modifier_cap` 钳制 |
| §7.3 仅服务端模式 B | 无自定义网络包；关键信息走聊天文本，客户端可不装 |

### 2.2 词库内容（`src/main/resources/data/chinese_oracle/`）

| 目录 | 内容 |
|------|------|
| `yi_ji/` | 11 条（宜 6：开矿/征伐/求财/渔猎/远行/营造；忌 5：忌动土/忌争斗/忌求财/忌远行/忌安床） |
| `signs/` | 8 条原创签文（覆盖 9 等吉凶范围，含诗/解签） |
| `fortune_tier/` | 未随包提供（9 等默认权重内置于代码，数据包可覆盖 weight/enabled） |

### 2.3 行动钩子（5 类全量）

| 玩法 | 事件 | 修正 |
|------|------|------|
| 挖掘速度 | `PlayerEvent.BreakSpeed` | `miningSpeedMul` |
| 输出伤害 | `LivingDamageEvent.Pre`（来源为玩家时） | `outgoingDamageMul` |
| 受伤 | `LivingIncomingDamageEvent`（目标为玩家时） | `incomingDamageMul` |
| 钓鱼 | `ItemFishedEvent` | `fishingLuckBonus`：宜渔猎时 30% 几率追加一条鳕鱼；忌时小概率移除 |
| 交易 | `PlayerContainerEvent.Open/Close`（MerchantMenu） | `tradePriceMul`：经 `specialPriceDiff` 临时调价，关闭/登出时还原 |

全部钩子首行守卫：`Config.ENABLED` + `enableMining`/`enableCombat`/`enableFishing`/`enableTrading` + `instanceof ServerPlayer`。

---

## 2b. Phase 2 — 内容与道具（已交付）

### 2b.1 道具与方块（§3.6）

| 注册项 | 名称 | 交互 |
|--------|------|------|
| `bamboo_sign_tube` | 竹签筒 | 右键查看今日签（聊天） |
| `almanac` | 黄历书 | 右键查看今日签（聊天） |
| `incense` | 香（改签消耗品） | 右键改签，成功则消耗 1 个 |
| `fortune_table` | 求签台（装饰方块） | 右键查看今日签 |

- 注册：`server/registry/ModItems`（方块/物品/创意标签「黄历运势」），钩子：`server/hook/ItemHooks`
- 模型/blockstate 使用原版占位贴图（无需二进制资源），后续可替换
- 改签消耗品由 `rerollRequireItem` 配置控制；次数上限 `rerollMaxPerDay`；未到新日先用香则先补签

### 2b.2 大凶庇护（§9.3 / §3.3）

- `server/fortune/Shelter`：玩家重生点/床半径内（同维度）判定，仅在命中时计算距离
- 挖矿、进出伤三个钩子统一走 `FortuneHooks.applyShelter`：吉凶达到 `shelterBadTierMin`（默认 `da_xiong`）且被庇护时，惩罚部分按 `shelterPenaltyScale` 缩放（默认 35%）
- 配置：`shelterEnabled` / `shelterRadius`(48) / `shelterBadTierMin` / `shelterPenaltyScale`

### 2b.3 权限抽象（§12.1）

- `server/permission/PermissionNodes`：7 个权限节点常量（command.* / bypass.penalty / reroll.unlimited）
- `server/permission/PermissionManager`：有 LuckPerms 时走反射桥（`LuckPermsBridge`），否则回退 OP 等级
- `/oracle player/set/reroll/reload/debug` 的 `requires` 全部改为走权限节点

> ⚠️ LuckPerms 桥为软依赖最佳努力实现（未装 LuckPerms 时自动回退），本项目无 LuckPerms 依赖，**该路径未经实际验证**。

### 2b.4 审计日志（§12.2）

- `/oracle set` / `/oracle reroll` 记录：`player / op / oldTier / newTier / dayIndex`（SLF4J INFO）

### 2b.5 词库扩充

- 签文 8 → 24 → **40**（覆盖 9 等吉凶全范围，原创两联 + 解签，达到 v1 ≥40 目标）
- `FortuneService.reroll` 改为返回 boolean（供消耗品/命令区分成功与否）

### 2b.6 交易/钓鱼钩子落地说明

- 交易：NeoForge 无 per-player 交易定价事件；采用 `specialPriceDiff` 临时调价（`PlayerContainerEvent.Open` 在 `openMenu` 末尾、`sendMerchantOffers` 之前触发，客户端与服务端价格一致），`Close`/`PlayerLoggedOutEvent` 还原。
  > ⚠️ **已知限制**：villager 的 offers 是共享对象，两名玩家同时交易同一村民时可能短暂互相看到对方的调价；`specialPriceDiff` 不持久化，异常断线时靠登出事件兜底还原。doc §3.3 已将 trading 标注为"谨慎"。
- 钓鱼：`ItemFishedEvent.getDrops()` 直接增删掉落，无共享状态，安全。

---

## 2c. 运营向 & 日切策略（已交付，全部聊天栏/控制台，无客户端）

### 2c.1 day_source 三模式（§6.1）

| 模式 | 说明 |
|------|------|
| `game_day`（默认） | overworld `getDayTime()/24000` |
| `wall_clock_utc` | 真实日期（UTC 天序号） |
| `wall_clock_offset` | 真实日期 + 时区偏移（`wallClockOffsetHours`，默认 +8 = 国服每日 0 点） |

`DayService.currentDayIndex` 按配置切换；日切检测与 RolloverQueue 逻辑对三模式通用。

### 2c.2 玩家策略（§7.1 / §7.5）

- `skipSpectator`（默认开）：旁观者不求签（返回中性签，不抽签不播报）
- `creativeBypass`（默认开）：创造玩家仍显示运势但不受惩罚（钩子取中性 Modifier）
- `skipBotNamePrefixes`（默认 `["[bot]"]`）：前缀匹配视为机器玩家，永不求签

### 2c.3 `/oracle share` 限频分享（§7.6）

- 全服广播自己今日摘要（吉凶 + 五行 + 签文首联），`shareCooldownSeconds` 限频（默认 60s）

### 2c.4 rollover 耗时 metrics（§8.7）

- `metrics=true` 时每 30s 输出一次日切队列 drain 滚动平均耗时（SLF4J INFO，Spark 友好）

---

## 2d. 黄历内容增强 — 时辰、节气与节日（§3.5，聊天栏展示）

| 项 | 实现 |
|----|------|
| 十二时辰 | `server/fortune/Shichen`：按游戏时刻（或 wall clock 小时）换算，子丑寅卯…亥；卯/午/酉为「吉」时辰 |
| 吉时辰小加成 | `shichenBonus` + `shichenBonusPercent`（默认 +3%），事件驱动仅作用于挖矿速度与钓鱼运气钩子，无每 tick 检测 |
| 二十四节气 | `server/fortune/SolarTerm`：按真实节气日期的**近似黄经表**（非均匀间距，0 = 立春）映射到游戏日 |
| 节日 | **数据包驱动**：`data/chinese_oracle/festivals/*.json`（`name`/`day_of_year`/`days`），内置 9 个（春节/元宵/清明/端午/七夕/中秋/重阳/冬至/除夕）；运营同名 JSON 可覆盖或新增，无需发新 jar |
| 节日加成 | `festivalBonus`（默认 +5%）小加成，事件驱动作用于挖矿/钓鱼 |
| 展示 | 运势播报与 `/oracle me` 标题行：`=== 今日黄历 · 节气 · 时辰（吉）· 节日 ===`；`showSolarTerm`/`showShichen` 可分别关闭 |

> 简化说明：节气按近似日期表映射（非天文黄经逐日计算），但比"均匀 15 天分段"更贴近真实分布；节日以 365 天游戏年、0 = 立春 为锚点。

---

## 2e. 命令补全 / 节日事件 / 数据包文档（已交付）

| 项 | 说明 |
|----|------|
| 裸 `/oracle` | 无参数时查看自己今日签（§3.7） |
| `/huangli` 别名 | `Commands.redirect` 指向 /oracle 命令树（§3.7） |
| `/oracle share` 清理 | 玩家登出时移除限频记录（不再随进程累积） |
| 节日事件（v2-lite） | `server/fortune/Festival`：按节气日序列简化映射 春节/元宵/清明/端午/七夕/中秋/重阳/冬至/除夕；标题行展示，`festivalBonus`+`festivalBonusPercent`（默认 5%）小加成（事件驱动，作用挖矿/钓鱼） |
| 数据包文档 | `src/main/resources/data/chinese_oracle/README.md`：fortune_tier/yi_ji/signs 格式与字段说明，供运营直接改 JSON |

---

## 2i. 词库扩充与平衡微调（已交付）

| 项 | 说明 |
|----|------|
| **宜忌条目权重** | `YiJiEntry` 新增 `weight`（JSON 可选，默认 1），`drawEntries` 改加权抽取——运营可调词条出现频率，纯数据包 |
| **新增宜忌** | 忌渔猎（fishing_luck -1.0）、忌祈福（incoming_damage +3%）；**忌安床**从纯展示改为 `rest_heal: -0.5`（醒来扣半颗心，轻惩罚，`enableRest` 可关） |
| **修复隐藏 bug** | 原 `drawEntries` 过滤逻辑导致「纯休息类词条（安眠/忌安床，无行动效果）」**永远抽不到**——改为无可行动效果即始终可抽，附回归测试 |
| **签文扩充** | 62 → **74**（新增 12 条，覆盖 忌渔猎/忌祈福/忌安床/安眠/祈福 主题，含 prefer 声明） |
| **README 同步** | `data/chinese_oracle/README.md` 补 `weight`、`rest_heal`、`prefer_yi/ji` 字段说明 |

**测试**：21 → **23**（新增 纯休息词条可抽取回归、条目权重抽取分布）。`./gradlew build` 全绿；服务器冒烟 ✅ `9 tiers / 15 yi-ji / 74 signs / 9 festivals`。

---

## 2h. 六项功能批量落地（已交付）

| 功能 | 实现 |
|------|------|
| **签文 × 宜忌联动（§3.4）** | `SignEntry` 新增 `prefer_yi`/`prefer_ji`；`drawSign` 加权抽签（命中当日宜忌权重 ×`signPreferWeight`，默认 3，0 关闭）；数据包 JSON 直接声明 |
| **节气逐日黄经（§3.5 v2）** | `SolarTerm` 改为太阳视黄经逐日计算（平均黄经 + 中心差），非均匀分布，测试验证锚点（立春 0 / 春分 44 / 夏至 137 / 秋分 231 / 冬至 321）与单调推进 |
| **五行极轻乘区（§3.5 v1）** | `WuxingEffect`：五行不再纯展示，挖矿/输出/钓鱼钩子获 `wuxingEffectPercent`（默认 2%）极轻加成，可关 |
| **词库扩充** | 宜忌 11→**13**（新增 祈福=受伤 -3%、安眠=`rest_heal`）；签文 40→**62**（新增 22 条，含 prefer 声明） |
| **安眠回血（rest 钩子）** | `Modifier` 新增 `restHeal` 字段 + `PlayerWakeUpEvent` 钩子，醒时按宜安眠回血（`enableRest` 可关） |
| **求签粒子（§10）** | 求签/改签/强制设签时服务端发轻量粒子（SMOKE + HAPPY_VILLAGER，原版粒子无客户端要求），`drawParticles` 可关 |
| **改签冷却** | `rerollCooldownSeconds`（默认 30s）每次改签冷却，与每日次数叠加；登出清理记录 |

**测试**：16 → **21** 个（新增 签文偏好权重、五行乘区、节气锚点/单调、restHeal）。`./gradlew build` 全绿；服务器冒烟 ✅ `9 tiers / 13 yi-ji / 62 signs / 9 festivals`。

---

## 2f. 单元测试（§8.8，已交付）

为满足文档「DrawPipeline 权重分布、seed 复现、跨日 keep_old、钳制」的测试要求：

- **纯函数化重构**：`DrawPipeline` / `ModifierTable.build` / `FortuneRegistry.drawTier` 改为显式传参（maxYi/maxJi / modifierCap / badProtection），不再读全局 Config；随机源由 MC `RandomSource` 换成 `java.util.Random`（同 seed 跨 JVM 可复现，逻辑零 MC 运行时依赖）
- **测试集**（JUnit 5，`./gradlew test`）：
  - `DrawPipelineTest`（5）：同 seed 确定性、tier 存在且签文区间匹配、宜忌 ActionType 不撞车、连凶保护避免大凶、`drawForTier` 保持档位
  - `ModifierTableTest`（5）：中性签→中性修正、宜挖掘 +13%、忌翻转 -13%、cap 钳制、弱档缩放
- 结果：**10/10 通过**；`./gradlew build` 含 test 全绿

> 注：本工程普通 JUnit 运行时不含游戏类（NeoForge ModDevGradle 行为），故纯逻辑测试完全脱离 MC 依赖；游戏内集成（假人 100 并发 / MSPT）仍需 Carpet 验证。

---

## 2g. ApricityUI 模组联动（§7.2 / §3.6，已交付）

### 版本确认（官方 Maven `maven.sighs.cc`）

用户给的是 `ApricityUI-forge-1.20.1:1.2.0`（Forge 1.20.1）；本工程为 **NeoForge 1.21.1**，经查官方仓库存在对应构件：

- 坐标：`com.sighs:ApricityUI-neoforge-1.21.1`
- 版本：`1.2.1`（latest，另有 1.2.0/1.2.0.2 等）
- 采用 `compileOnly` 引入（软依赖，不打包、不冲突）

### 联动内容

ApricityUI 是客户端 HTML/CSS/JS UI 库（`ApricityScreen`/`Document`/DOM API）。联动方式：**服务端 → 客户端 S2C 同步运势 + AUI 全屏黄历页面**，聊天栏保留为兜底。

| 组件 | 说明 |
|------|------|
| `common/network/FortuneDisplay` | S2C payload（dayIndex/tier/宜忌名/签文/五行/节气/时辰/节日），手动 StreamCodec |
| `common/network/AuiHandshakeC2S` | C2S 握手：登录时告知服务端「客户端是否装了 ApricityUI」 |
| `network/ModPayloads` | 注册 playToClient（FortuneDisplay）+ playToServer（AuiHandshakeC2S） |
| `client/network/AuiHandshakeSender` | `ClientPlayerNetworkEvent.LoggingIn` 时发握手（`PacketDistributor.sendToServer`） |
| `server/network/FortuneNetwork` | AUI 客户端集合 + **路由**：有 AUI → 只发包不弹聊天；无 AUI/无客户端 → 聊天展示（模式 B） |
| `client/network/ClientPayloadHandler` | 收包后 `enqueueWork`，`ModList.isLoaded("apricityui")` 守卫 |
| `client/integration/ApricityIntegration` | **ApricityScreen 全屏界面**：`Minecraft.getInstance().setScreen(...)`，`init()` 里 `super.init()` + DOM 填充 |
| `assets/apricityui/apricity/screens/fortune.html` | **紧凑圆角国风黄历卡**：宣纸色、朱红/鎏金/墨色、楷体/宋体字栈；展示服务器名「食韵筑家Ⅲ · 烟火长歌」与 Minecraft 灯笼纹理；宜/忌双栏、签文分区、空词条“暂无”；圆角卡片/纸面/标签/按钮；印章按单字/四字吉凶动态调整字号；动画为卡片入场、印章盖下、信息块浮现；“展开解签/收起解签”轻交互；`mode=browser` 自适应 + 默认 2.5x 缩放居中 |

- `mods.toml` 声明可选客户端依赖：`apricityui` optional，`[1.2.1,)`，`side=CLIENT`
- 软依赖安全：AUI 类只在 `ApricityIntegration` 中引用，且仅在 `isLoaded` 后加载；无 AUI 或无本 mod 客户端时 payload 被忽略
- **有 AUI 不弹聊天**：客户端登录握手（`AuiHandshakeC2S`）→ 服务端对该玩家只发 Screen 包；无 AUI 玩家保持聊天展示；控制台执行命令仍走聊天
- 遵循官方 `docs/ai-skill.md`（宿主选择、meta、Ore 主题完整资料）
- **UI 交互调试（MCP 实机验证）**：「展开解签」监听由 Java 侧 `Element.addEventListener` 绑定（页面内联脚本在 AUI 中未生效，已弃用）；展开动画用纯 CSS `@keyframes`（基础 `height:0` + 动画到 44px，消除闪缩）；国风卡片圆角、服务器名「食韵筑家Ⅲ · 烟火长歌」、灯笼纹理、印章动态字号、页脚间距等均经 MCP（ws://127.0.0.1:25321/apricity + debug.json 凭据）点击/采样实测确认
- ⚠️ 开发注意：`run/mods` 若被同步进 ApricityUI jar，专用服会因 AUI 为客户端 mod 而加载失败，删除该 jar 即可（run/ 已 gitignore）

### 验证

- ✅ `./gradlew build` 通过，16 个单元测试全绿
- ✅ 专用服务器冒烟启动无异常（payload 注册、可选依赖声明、握手路由正常）
- ⚠️ 客户端界面与 AUI 渲染需真实客户端 + 安装 ApricityUI 后实机验证（本环境无法无头测试）

---

## 3. 验证结果

| 项 | 结果 |
|----|------|
| `./gradlew compileJava` | ✅ 通过（修正 3 处 NeoForge 21.1 API 用法，见 §5） |
| `./gradlew build` | ✅ BUILD SUCCESSFUL，jar 含全部 class / mixins / lang / data |
| `./gradlew runServer` 冒烟 | ✅ `Done (1.619s)`；`Fortune registry loaded: 9 tiers, 11 yi/ji entries, 8 signs`；无异常 |
| 配置生成 | ✅ `run/config/chinese_oracle-common.toml` 正常生成（含 shelter/reroll 新项）并热监控 |
| 附件注册 | ✅ `fortune_data` Attachment 正常注册（加载阶段无报错） |
| 物品/方块注册 | ✅ 4 物品 + 1 方块 + 创意标签正常注册，模型/blockstate 打入 jar |
| 端口 | 冒烟改用 25566（本机 25565 被既有 NeoForge 21.1.247 服务器占用），测后已还原 |

---

## 4. 与设计文档的偏差 / 决策记录

1. **加载器**：按已有工程模板采用 **NeoForge**（设计文档推荐 Fabric 优先，但当前脚手架为 NeoForge 1.21.1；架构章节与 Phase 1 内容可平移）。
2. **配置格式**：用 NeoForge `ModConfigSpec`（`chinese_oracle-common.toml`）而非文档 §5.3 示例的 `server.json`——后者为示意；如需 JSON 配置可在后续替换。
3. **聊天展示**：服务端中文字面量（§11 主语言简体中文）；`tier.*` 语言键已预置（en/zh），待客户端 GUI 阶段启用 translatable。
4. **五行**：仅随机展示（符合 v1 约定），无数值乘区。
5. **未挂钩子的行动**：所有 8 个 ActionType 的修正均已预计算；落地钩子为 mining/combat/fishing/trading（5 处）。building/explore/enchanting/rest 仅展示（轻/谨慎项，见 §3.3）。
6. **权限**：Phase 2 已引入权限节点抽象（LuckPerms 反射桥 + OP 回退）；未装 LuckPerms 时仍等价于 OP 2 级。
7. **改签**：服务端按 `rerollMaxPerDay` 校验次数；`rerollRequireItem=true` 时消耗香（`chinese_oracle:incense`）。
8. **庇护（shelter）、时辰、节气、GUI、自定义网络包**：按文档分期，属 Phase 2/3/4。
9. **day_source**：§2c.1 已实现三模式（`game_day` / `wall_clock_utc` / `wall_clock_offset`）。
10. **下下大凶**：默认启用（权重 1），可通过 `fortune_tier/` 数据包关闭（代码支持，未随包提供示例）。

---

## 5. 开发中修正的 API 用法（备查）

| 问题 | 修正 |
|------|------|
| `AttachmentType.serializable(...)` 返回 `Builder`，不能直接注册 | 补 `.build()` |
| `ResourceManager` 包路径错误 | 应为 `net.minecraft.server.packs.resources.ResourceManager` |
| `listResources` 谓词参数类型 | 谓词接收 `ResourceLocation`，需用 `p.getPath().endsWith(".json")` |
| `Map.copyOf` 生成不可变 map，加载期 `put` 抛异常 | 构造器改为可变 `LinkedHashMap` 副本 |
| `ModConfigSpec.DoubleValue.get()` 装箱 `Double` | 用 `.floatValue()` |

---

## 6. 遗留事项与风险

### 待办（Phase 2 剩余）
- [x] 村民交易价格、钓鱼运气钩子（§2b.6，已落地）
- [ ] 求签台/签筒 GUI（用户明确暂不需要，聊天栏实现即可）
- [x] 词库扩充至 40 签文（已达 v1 目标）
- [ ] LuckPerms 桥接实机验证（本环境未装 LuckPerms）
- [ ] 交易调价共享 offers 的并发问题（如需彻底解决需重写 villagers 交易或 mixin）
- [x] `/oracle share` 记录在登出时清理（§2e，已落地）

### 待办（Phase 3/4）
- [ ] 黄历 GUI / HUD / 键位（原生 GUI 暂缓；**已用 ApricityUI 联动替代**，§2g）
- [x] ApricityUI 联动（§2g，已交付；客户端实机验证待 AUI 环境）
- [ ] 节气按真实黄经逐日计算（当前为近似日期表，§2d）
- [ ] Spark 指标深入集成（`metrics` 已提供基础日志）
- [ ] 游戏内集成压测（Carpet 假人 100 登录 / MSPT，§8.8；单元测试已交付 16 个）

### 已知风险
- 玩家数据在附件中随玩家存档保存；如需"奶桶不清签档"已天然满足（NBT 不绑定 potion）。
- 日切队列中 `pending_policy` 未配置（当前为"保持昨日签直到新签完成"，与推荐 `keep_old` 一致）。
- 冒烟测试无在线玩家，多人并发（100 假人）与 MSPT 压测待 Carpet 假人验证（§8.8）。
- 道具为占位贴图（复用原版材质），`fortune_table` 为纯装饰方块、无碰撞自定义模型。

---

*本报告与工程同目录，记录至 Phase 1 交付点。*
